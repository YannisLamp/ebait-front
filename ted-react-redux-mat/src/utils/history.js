import { createBrowserHistory } from 'history';

// Create and export History object so that we can have correct 
// browser history with redirection from redux actions
export const history = createBrowserHistory();